setTimeout(function() {
	introJs().start();
}, 3000);


// get introjs to work on videos/index by copying whats on videomasterindex/alphabetical, change the application 
// header layout and change routes so that videos/index is root 