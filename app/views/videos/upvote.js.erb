Rails.$('.random-book')[0].innerHTML = ("<%= j (render partial: 'book', locals: { book: @video }) %>")


// Rails.$('#video-<%= @video.id %>').innerHTML = ("<%= j (render partial: 'book', locals: { book: @video }) %>")
